[project]
name = "myngrok"
version = "0.1.0"
description = "Minimal ngrok-style TCP tunnel over WebSockets"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "websockets>=12.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
